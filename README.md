# Angular_assignment1.2
 Install Angular cli through npm.  Check angular cli version.  Create new project with Angular CLI through cmd.  Run the project and navigate to the http://localhost:4200/
